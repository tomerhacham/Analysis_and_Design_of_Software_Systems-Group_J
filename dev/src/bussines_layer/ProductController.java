package bussines_layer;

public class ProductController {
    //fields
    private final PIDAllocator PIDAllocator;

    public ProductController(PIDAllocator PIDAllocator) {
        this.PIDAllocator = PIDAllocator;
    }
}
